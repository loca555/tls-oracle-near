# NEAR
NEAR_NETWORK=testnet
NEAR_NODE_URL=https://test.rpc.fastnear.com
TLS_ORACLE_CONTRACT=tls-oracle.testnet

# Prover Service
PROVER_URL=http://127.0.0.1:7048

# Notary Server
NOTARY_PORT=7047
NOTARY_BIND=127.0.0.1
NOTARY_KEY_PATH=notary_key.bin
# CORS: разрешённый origin (по умолчанию — только Prover)
# ALLOWED_ORIGIN=http://127.0.0.1:7048

# Prover Service
PROVER_PORT=7048
PROVER_BIND=127.0.0.1
NOTARY_URL=http://127.0.0.1:7047
# CORS: разрешённый origin (по умолчанию — только Backend)
# ALLOWED_ORIGIN=http://127.0.0.1:4001

# Backend
PORT=4001
# CORS: URL фронтенда (в production пустой — same origin)
# FRONTEND_URL=http://localhost:3001
