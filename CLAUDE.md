Always respond in Russian.

# Стиль кода
- Комментарии и docstrings на русском языке
- Не трогать файлы в venv/
- Не коммитить файлы из ENV/ (секреты)
- Бэкапы не редактировать (backup-*)

# Безопасность
- Никогда не выводить API-ключи и токены в код или логи
- Файл ENV содержит секреты — не читать и не модифицировать
- Не менять торговую логику без явного подтверждения

# Предпочтения
- Перед изменением торговой логики — всегда спрашивать подтверждение
- При ошибках — сначала проверять логи
- Тесты запускать через venv: ./venv/bin/python -m pytest
